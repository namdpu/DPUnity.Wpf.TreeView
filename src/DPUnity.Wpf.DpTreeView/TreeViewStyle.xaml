<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:hc="https://handyorg.github.io/handycontrol"
                    xmlns:icons="https://dpunity.com/dev/wpf/ui/controls/packicon"
                    xmlns:ftv="clr-namespace:DPUnity.Wpf.DpTreeView">
    <ResourceDictionary.MergedDictionaries>
        <hc:IntellisenseResources Source="/HandyControl;Component/DesignTime/DesignTimeResources.xaml" />
        <hc:Theme />
    </ResourceDictionary.MergedDictionaries>


    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    <SolidColorBrush x:Key="PrimaryBrush60"
                     Opacity="0.6"
                     Color="#03AB54" />

    <Style x:Key="DP_FilterTreeViewItemStyle"
           TargetType="{x:Type TreeViewItem}">
        <Setter Property="HorizontalAlignment"
                Value="Stretch" />
        <Setter Property="Visibility"
                Value="{Binding IsVisible, Mode=TwoWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
        <Setter Property="IsExpanded"
                Value="{Binding IsExpanded, Mode=TwoWay}" />
        <Setter Property="Margin"
                Value="0" />
        <Setter Property="HorizontalContentAlignment"
                Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
        <Setter Property="VerticalContentAlignment"
                Value="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TreeViewItem}">
                    <StackPanel HorizontalAlignment="Stretch">
                        <Grid>
                            <Border x:Name="ItemBackground"
                                    Background="{DynamicResource SecondaryRegionBrush}"
                                    Margin="-10000 0 0 0"
                                    HorizontalAlignment="Stretch" />
                            <StackPanel HorizontalAlignment="Stretch"
                                        Orientation="Horizontal">
                                <!-- Removed IsHitTestVisible="False" -->
                                <ToggleButton x:Name="Expander"
                                              Style="{StaticResource {x:Static ToolBar.ToggleButtonStyleKey}}"
                                              IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                              ClickMode="Press">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                                            <Border Width="16"
                                                    Height="16"
                                                    Background="Transparent">
                                                <icons:PackIcon x:Name="ExpandIcon"
                                                                HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"
                                                                Kind="ChevronRight" />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsChecked"
                                                         Value="True">
                                                    <Setter TargetName="ExpandIcon"
                                                            Property="Kind"
                                                            Value="ChevronDown" />
                                                    <Setter TargetName="ExpandIcon"
                                                            Property="Margin"
                                                            Value="0 0 0 4" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <ContentPresenter Grid.Column="1"
                                                  x:Name="PART_Header"
                                                  Margin="5 0 50 0"
                                                  ContentSource="Header"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                <!-- ContentPresenter needs to receive clicks for CheckBox -->
                            </StackPanel>
                        </Grid>
                        <ItemsPresenter x:Name="ItemsHost"
                                        Grid.Row="1"
                                        Grid.Column="1"
                                        Margin="19 0 0 0"
                                        Visibility="Collapsed" />
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasItems"
                                 Value="False">
                            <Setter TargetName="Expander"
                                    Property="Visibility"
                                    Value="Hidden" />
                        </Trigger>
                        <Trigger Property="IsExpanded"
                                 Value="True">
                            <Setter TargetName="ItemsHost"
                                    Property="Visibility"
                                    Value="Visible" />
                        </Trigger>

                        <Trigger SourceName="ItemBackground"
                                 Property="IsMouseOver"
                                 Value="True">
                            <Setter TargetName="ItemBackground"
                                    Property="Background"
                                    Value="{DynamicResource ThirdlyRegionBrush}" />
                        </Trigger>
                        <Trigger Property="IsSelected"
                                 Value="True">
                            <Setter TargetName="ItemBackground"
                                    Property="Background"
                                    Value="{DynamicResource PrimaryBrush60}" />
                        </Trigger>
                        
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DP_HighlightTextBlockStyle"
           TargetType="hc:HighlightTextBlock">
        <Setter Property="Foreground"
                Value="{DynamicResource PrimaryTextBrush}" />
        <Setter Property="SourceText"
                Value="{Binding Text}" />
        <Setter Property="QueriesText"
                Value="{Binding Path=ItemsSource.FilterString, RelativeSource={RelativeSource AncestorType={x:Type TreeView}}}" />
        <Setter Property="HighlightBrush"
                Value="{DynamicResource PrimaryBrush}" />
        <Setter Property="HighlightTextBrush"
                Value="White" />
        <Setter Property="VerticalAlignment"
                Value="Center" />
        <Setter Property="Foreground"
                Value="{Binding Foreground}" />
        <Setter Property="FontSize"
                Value="{Binding FontSize}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding Foreground}"
                         Value="{x:Null}">
                <Setter Property="Foreground"
                        Value="{DynamicResource PrimaryTextBrush}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="DP_FilterTreeViewStyle"
           TargetType="TreeView">
        <Setter Property="ItemContainerStyle"
                Value="{StaticResource DP_FilterTreeViewItemStyle}" />
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel IsItemsHost="True" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="VirtualizingPanel.VirtualizationMode"
                Value="Recycling" />
        <Setter Property="VirtualizingPanel.IsVirtualizing"
                Value="True" />
        <Setter Property="Background"
                Value="{DynamicResource SecondaryRegionBrush}" />
        <Setter Property="BorderBrush"
                Value="{DynamicResource BorderBrush}" />
        <Setter Property="BorderThickness"
                Value="1" />
        <Setter Property="ftv:TreeViewBehavior.IsBehaviorEnabled"
                Value="True" />
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <hc:HighlightTextBlock Style="{DynamicResource DP_HighlightTextBlockStyle}" />
                </HierarchicalDataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TreeView">
                    <Grid Background="{DynamicResource RegionBrush}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1">
                            <hc:SimpleStackPanel 
                                                 Margin="5"
                                                 Orientation="Horizontal">
                                <Button Style="{DynamicResource DP_IconButtonWithoutBorder}"
                                        Content="ExpandAll"
                                        ToolTip="Expand All"
                                        Command="{Binding (ftv:TreeViewBehavior.ExpandAllCommand), RelativeSource={RelativeSource AncestorType=TreeView}}"
                                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=TreeView}}" />
                                <Button Style="{DynamicResource DP_IconButtonWithoutBorder}"
                                        Content="CollapseAll"
                                        ToolTip="Collapse All"
                                        Command="{Binding (ftv:TreeViewBehavior.CollapseAllCommand), RelativeSource={RelativeSource AncestorType=TreeView}}"
                                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=TreeView}}" />
                                <hc:Divider Orientation="Vertical" />
                                <TextBlock Text="Expand level: "
                                           VerticalAlignment="Center"
                                           Margin="5 0" />
                                <ComboBox x:Name="ExpandLevelComboBox"
                                          Style="{DynamicResource ComboBox.Small}"
                                          ItemsSource="{Binding (ftv:TreeViewBehavior.ExpandLevels), RelativeSource={RelativeSource TemplatedParent}}"
                                          SelectedItem="{Binding (ftv:TreeViewBehavior.SelectedExpandLevel), RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" />
                                <hc:Divider Orientation="Vertical" />
                            </hc:SimpleStackPanel>
                        </Border>
                        <Border Grid.Row="1"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Background="{TemplateBinding Background}">
                            <ScrollViewer Focusable="False"
                                          CanContentScroll="True"
                                          HorizontalScrollBarVisibility="Visible"
                                          VerticalScrollBarVisibility="Auto"
                                          PanningMode="Both">
                                <ItemsPresenter />
                            </ScrollViewer>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DP_TreeViewContainIcon"
           TargetType="{x:Type hc:CheckTreeView}"
           BasedOn="{StaticResource DP_FilterTreeViewStyle}">
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <StackPanel Orientation="Horizontal" >
                        <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Left">
                            <CheckBox.Template>
                                <ControlTemplate TargetType="{x:Type CheckBox}">
                                    <Border x:Name="bd"
                                            hc:BorderElement.Circular="True"
                                            Height="22"
                                            Width="22"
                                            BorderBrush="{DynamicResource BorderBrush}"
                                            BorderThickness="1">
                                        <icons:PackIcon x:Name="icon"
                                                        Width="16"
                                                        Height="16"
                                                        Margin="-4 0 0 0">
                                        </icons:PackIcon>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked"
                                                 Value="True">
                                            <Setter TargetName="icon"
                                                    Property="Kind"
                                                    Value="Lightbulb" />
                                            <Setter TargetName="icon"
                                                    Property="Foreground"
                                                    Value="{DynamicResource LightOnBrush}" />
                                            <Setter TargetName="bd"
                                                    Property="Background"
                                                    Value="Transparent" />
                                        </Trigger>

                                        <Trigger Property="IsChecked"
                                                 Value="False">
                                            <Setter TargetName="icon"
                                                    Property="Kind"
                                                    Value="LightbulbOutline" />
                                            <Setter TargetName="bd"
                                                    Property="Background"
                                                    Value="Transparent" />
                                            <Setter TargetName="icon"
                                                    Property="Foreground">
                                                <Setter.Value>
                                                    <SolidColorBrush Color="{DynamicResource LightOffColor}"
                                                                     Opacity="0.2" />
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>

                                        <Trigger Property="IsChecked"
                                                 Value="{x:Null}">
                                            <Setter TargetName="icon"
                                                    Property="Kind"
                                                    Value="LightbulbMultipleOutline" />
                                            <Setter TargetName="icon"
                                                    Property="Foreground">
                                                <Setter.Value>
                                                    <SolidColorBrush Color="{DynamicResource LightOffColor}" />
                                                </Setter.Value>
                                            </Setter>
                                            <Setter TargetName="bd"
                                                    Property="Background">
                                                <Setter.Value>
                                                    <SolidColorBrush Color="{DynamicResource LightOnColor}" Opacity="0.4"/>
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </CheckBox.Template>
                        </CheckBox>
                        <hc:HighlightTextBlock Style="{DynamicResource DP_HighlightTextBlockStyle}"
                                               Margin="4 2"
                                               FontSize="14" 
                                               SourceText="{Binding Text}"
                                               VerticalAlignment="Center"
                                               QueriesText="{Binding Path=ItemsSource.FilterString, RelativeSource={RelativeSource AncestorType={x:Type TreeView}}}" />
                    </StackPanel>
                </HierarchicalDataTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>